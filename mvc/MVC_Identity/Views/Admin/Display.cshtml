@using MVC_Identity.Domain;
@{
}

<div class="row">
    <div class="col-md-10">
        <a class="btn btn-success" style="margin-bottom:10px" onclick="ShowPopup('@Url.Action("AddEditUser","Admin")')"><i class="fa fa-plus"></i> Add New User</a>
    </div>
</div>

<div class="container" style="margin-top:10px;">
    <table id="user_grid" class="table table-striped table-bordered" style="width:100%">
    <thead>
        <tr>
            <th>Name</th>
            <th>Username</th>
            <th>Email</th>
            <th>Phone Number</th>
            <th></th>
        </tr>
    </thead>
</table>
</div>

@section Scripts{
    @await Html.PartialAsync("_ValidationScriptsPartial")
    <script src="~/js/views/display/index.js"></script>
}
