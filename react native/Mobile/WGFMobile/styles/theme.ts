import { Theme } from "@react-navigation/native";
import React from "react";
import { DefaultTheme, Colors } from "react-native-paper";

const applicationTheme = (theme: Theme) => ({
    ...theme
});
export default applicationTheme;
